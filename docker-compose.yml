services:
  web:
    build: .
    container_name: dynamic-resume-app
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always
